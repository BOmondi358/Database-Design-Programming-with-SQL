-- Creating a new database named salesDB
CREATE DATABASE salesDB;

-- Dropping the database named demo if it exists
DROP DATABASE IF EXISTS demo;

/*CREATE DATABASE salesDB; → This command creates a new database named salesDB.
DROP DATABASE IF EXISTS demo; → This command first checks if demo exists and deletes it if it does, preventing errors if the database does not exist.
*/
